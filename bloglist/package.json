{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix bloglist-frontend\"",
    "build": "npm run build --prefix bloglist-frontend",
    "start": "NODE_ENV=production npm start --prefix server",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
