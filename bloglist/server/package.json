{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "test:conc": "cross-env  NODE_ENV=test node --test-concurrency=1 --test",
    "test:only": "cross-env  NODE_ENV=test node --test-only --test",
    "start:test": "cross-env NODE_ENV=test node --watch index.js"  
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.4.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
